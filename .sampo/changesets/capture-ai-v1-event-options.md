---
pypi/posthog-sdk-test-harness: minor
---

Add an opt-in `capture_ai_v1` suite for `POST /i/v1/ai/events`, and opt-in `event_options` tests for the caller options contract on both V1 capture suites.
