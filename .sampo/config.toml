# Sampo configuration
version = 1

[git]
default_branch = "main"
short_tags = "posthog-sdk-test-harness"  # Tag with v1.2.3 rather than posthog-sdk-test-harness-v1.2.3

[github]
repository = "posthog/posthog-sdk-test-harness"

[changelog]
# Options for release notes generation.
# show_commit_hash = true (default)
# show_acknowledgments = true (default)

[packages]
# Options for package discovery and filtering.
# ignore_unpublished = false (default)
# ignore = ["internal-*", "examples/*"]
